<%= render partial: 'product', object: @product,
           locals: {show_details_link: false} %>

<p><%= @product.description %></p>

<p>
  <% @product.stocks.each do |stock| %>
    <p><%= stock.size.size %> | <%= stock.colour.colour %> <%= link_to 'Add Order', orders_path %></p>
  <% end %>
</p>
